<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>saml-authentication</artifactId>
        <groupId>org.gromozeka.teamcity</groupId>
        <version>1.3.2</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>build</artifactId>

    <packaging>pom</packaging>

    <properties>
        <maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
        <teamcity.saml-authentication.plugin.version>${version}</teamcity.saml-authentication.plugin.version>
        <teamcity.saml-authentication.plugin.vendorName>Morincer</teamcity.saml-authentication.plugin.vendorName>
        <teamcity.saml-authentication.plugin.vendorUrl>http://github.com/morincer</teamcity.saml-authentication.plugin.vendorUrl>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.gromozeka.teamcity</groupId>
            <artifactId>saml-authentication-server</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>com.google.code.maven-replacer-plugin</groupId>
                <artifactId>replacer</artifactId>
                <version>1.5.2</version>
                <executions>
                    <execution>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>replace</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <file>${basedir}/../teamcity-plugin.xml</file>
                    <outputFile>${basedir}/target/teamcity-plugin.xml</outputFile>
                    <replacements>
                        <replacement>
                            <!--suppress UnresolvedMavenProperty -->
                            <token>@Version@</token>
                            <value>${teamcity.saml-authentication.plugin.version}</value>
                        </replacement>
                        <replacement>
                            <!--suppress UnresolvedMavenProperty -->
                            <token>@VendorName@</token>
                            <value>${teamcity.saml-authentication.plugin.vendorName}</value>
                        </replacement>
                        <replacement>
                            <!--suppress UnresolvedMavenProperty -->
                            <token>@VendorURL@</token>
                            <value>${teamcity.saml-authentication.plugin.vendorUrl}</value>
                        </replacement>
                    </replacements>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.4</version>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <finalName>saml-authentication</finalName>
                            <outputDirectory>${project.build.directory}/../../target/</outputDirectory>
                            <appendAssemblyId>false</appendAssemblyId>
                            <descriptors>
                                <descriptor>plugin-assembly.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jetbrains.teamcity</groupId>
                <artifactId>teamcity-sdk-maven-plugin</artifactId>
                <version>0.4.1</version>
                <configuration>
                    <teamcityDir>../servers/${teamcity-version-runtime}</teamcityDir>
                    <pluginPackageName>saml-authentication.zip</pluginPackageName>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
